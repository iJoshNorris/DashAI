import React from "react";
import { DialogContent, Typography } from "@mui/material";

const TaskSelectorNode = () => (
  <DialogContent>
    <Typography variant="h6" component="div">
      Task Selector
    </Typography>
  </DialogContent>
);

export default TaskSelectorNode;
